<?xml version="1.0" encoding="utf-8"?>
<!--
  v2.2.40: dataExtractionRules untuk Android 12+ — melengkapi
  android:allowBackup="false". Menegaskan TIDAK ada data aplikasi yang
  ikut backup cloud / device-to-device transfer. Uninstall = data
  benar-benar terhapus → install baru = fresh → dialog "Data Ditemukan"
  muncul (bukan skip ke PIN pad karena data zombie dari auto-backup).
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
